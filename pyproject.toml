[project]
name = "neurovolume-testing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11" 
dependencies = [
    "bpy>=5.0.1",
    "nibabel>=5.4.2",
]
